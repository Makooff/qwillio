// Font family name used across the composition. The actual @font-face load is
// performed by <FontLoader/> (see components.tsx) inside the React tree, which
// is the reliable pattern for concurrent Remotion renders.
export const FONT_FAMILY = 'Outfit, system-ui, sans-serif';
